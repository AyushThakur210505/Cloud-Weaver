{
  "name": "project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @cloudweaver/schema && concurrently -n frontend,backend -c blue,green \"npm run dev -w @cloudweaver/frontend\" \"npm run dev -w @cloudweaver/backend\"",
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
